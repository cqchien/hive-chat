export enum GroupType {
  DIRECT = 'direct',
  GROUP = 'group',
}

export enum GroupMemberRole {
  ADMIN = 'admin',
  MEMBER = 'member',
}

export enum GroupMemberStatus {
  ACTIVE = 'active',
  LEFT = 'left',
  BANNED = 'banned',
  MUTED = 'muted',
}
